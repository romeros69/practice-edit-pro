ГСЧ = Новый ГенераторСлучайныхЧисел;
Мас = Новый Массив(100);
Минимум = 1001;
Максимум = -1;
Сум = 0;
Для Шаг = 0 По 99 Цикл
	Мас[Шаг] = ГСЧ.СлучайноеЧисло(0, 1000);
	Сум = Сум + Мас[Шаг];
	Если Мас[Шаг] < Минимум Тогда
		Минимум = Мас[Шаг];
	КонецЕсли;
	Если Мас[Шаг] > Максимум Тогда
		Максимум = Мас[Шаг];
	КонецЕсли;
КонецЦикла;
СрЗнач = Сум / 100;
Сообщить("Минимальное значение = " + Минимум + " Максимальное значение = " + Максимум + " Среднее значение = " + СрЗнач);